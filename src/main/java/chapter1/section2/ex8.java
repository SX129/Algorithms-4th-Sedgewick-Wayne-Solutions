package chapter1.section2;

public class ex8 {
    /* 1.2.8 Suppose that a[] and b[] are each integer arrays consisting of millions of integers.
    * What does the following code do? Is it reasonably efficient? */
    public static void main(String[] args){
        int[] a;
        int[] b;

        //int t = a;
        //a = b;
        //b = t;

        // The code swaps the array references of variables a and b. It is reasonably efficient as the code copies the references rather than recreating them.

    }
}
